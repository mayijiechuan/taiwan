<p id="notice"><%= notice %></p>

<h1><%= params[:year]%>年台湾</h1>

<table>


  <tbody>
    <% @ideas.in_groups_of(2).each do |ideas| %>
      <tr>
        <% ideas.each do |idea| %>
        <% next unless idea%>
        <td><%= idea.name %></td>
        <td><%= idea.description %></td>
        <td><%= image_tag idea.picture.url(:thumb) if idea.picture? %></td>

        <td><%= link_to 'Show', idea %></td>
        <td><%= link_to 'Edit', edit_idea_path(idea) %></td>
        <td><%= link_to 'Destroy', idea, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to '新規作成', new_idea_path %>
<%= link_to 'peach', "http://www.flypeach.com/jp/ja-jp/homeJP.aspx", class: "peach"%>
